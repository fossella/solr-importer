# Copy this file to .env and fill in real values. .env itself is
# gitignored - only .env.example should be checked in.

# --- MySQL / datasource connection ---
DB_DSN="mysql:host=127.0.0.1;dbname=shop;charset=utf8mb4"
DB_USER=solr
DB_PASSWORD=

# --- Solr connection ---
SOLR_HOST=localhost
SOLR_PORT=8983
SOLR_CORE=products
